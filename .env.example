# Flask Configuration
SECRET_KEY=genera-una-chiave-casuale-lunga-qui
FLASK_ENV=production

# Genera SECRET_KEY con: python -c "import secrets; print(secrets.token_hex(32))"

# Gunicorn WSGI server (production)
# GUNICORN_BIND=0.0.0.0:5000
# GUNICORN_WORKERS=3
# GUNICORN_LOG_LEVEL=info

# SNMP trap notifications (Zabbix)
# ZABBIX_HOST=zabbix.example.com
# SNMP_COMMUNITY=public
# SNMP_PORT=162

# Reverse proxy (set to the IP(s) of your load-balancer / Nginx)
# FORWARDED_ALLOW_IPS=127.0.0.1
